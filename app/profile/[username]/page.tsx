import { PageCard } from "@/components/page-card";

export default function ProfilePage() {
  return <PageCard title="Player Profile">ELO, credits, stats, match history, linked Minecraft account.</PageCard>;
}
